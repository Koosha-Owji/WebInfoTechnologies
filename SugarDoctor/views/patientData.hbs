<h1>Displaying Patient Data</h1>
<ul>
    {{#each data}}
    <li> {{this.first_name}}, {{this.last_name}} </li>
    {{!-- {{Check if the data has been added today, display check if true and cross if not}} --}}
    {{#if this.glucose_today}} Glucose added today {{this.glucoseLevel}}
        {{#if (isGreater this.glucoseLevel this.glucoseUpper)}} Greater than upper 
        {{else if (isLess this.glucoseLevel this.glucoseLower)}} Less than lower
        {{else}} Glucose at healthy level
        {{/if}}
        
    {{else}}
        Glucose Not added {{this.glucoseLevel}}
         {{#if (isGreater this.glucoseLevel this.glucoseUpper)}} yesterday Greater than upper 
        {{else if (isLess this.glucoseLevel this.glucoseLower)}} yesterday Less than lower
        {{else}} yesterday Glucose at healthy level
        {{/if}}
    {{/if}}

    {{/each}}
</ul>